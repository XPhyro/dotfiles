# TODO: Add pacmixer/ncpamixer/pulsemixer.
# TODO: Use fn+KEY on k380.

### Programs ###
##
#

super + shift + Return
    $TERMINAL
super + p
    dmenu_run
ctrl + alt + {h, n, w, f, t, i, s, p, g}; ctrl + alt + t
    $TERMINAL -e sudo {htop, nvtop, jnettop, iftop, iptraf, iotop, slabtop, powertop, glances}
ctrl + alt + m; ctrl + alt + {m, s}
    {$TERMINAL -e neomutt; statbarsetmail, lcknotiflog && notify-send -t 1000 "Syncing mail" && syncmail && lcknotiflog && notify-send -t 1000 "Synced mail"}
ctrl + alt + r; ctrl + alt + {r, s}
    $TERMINAL -e {ranger "$HOME", newsboat}
ctrl + alt + w; ctrl + alt + w
    $TERMINAL -e weather
ctrl + alt + b; ctrl + alt + b
    $TERMINAL -e bluetoothctl
ctrl + alt + r; ctrl + alt + {q, w}
    {hsetroot; restart-wallpaper -T off, restart-wallpaper -fT on}
ctrl + alt + s; ctrl + alt + {s, d}
    {cpscr, flameshot gui}
ctrl + alt + o; ctrl + alt + {t, b, i, c, s, j, q, 1}
    {tglapp stalonetray, $BROWSER, $BROWSER --incognito, tglapp lightcord, $BROWSER --new-window "http://localhost:8989/", $BROWSER --new-window "http://localhost:9117/", $BROWSER --new-window "http://localhost:24589", $BROWSER --new-window "192.168.1.1"}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + f; ctrl + alt + {i, p}
    firefox {_, --private-window}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + c; ctrl + alt + {i, p, a}
    {google-chrome-stable, google-chrome-stable --incognito, $BROWSER --new-window "https://calendar.google.com/calendar"}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + {p, z, n}
    {popcorntime, open-zoom, nestopia}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + d; ctrl + alt + {i}
    {dictionary}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + b; ctrl + alt + {w}
    {bitwarden}
ctrl + alt + o; ctrl + alt + p; ctrl + alt + r; ctrl + alt + {e}
    {xdg-reopen}
ctrl + alt + o; ctrl + alt + g; ctrl + alt + {s, g, m, w, c}
    {steam, open-steam, tglapp ModOrganizer2, wowstart, wowsim}
ctrl + alt + o; ctrl + alt + d; ctrl + alt + {u, r, d, l, c}
    {unity-hub, rider, unity-hub & rider &, unity-layout-reference, clion}
ctrl + alt + o; ctrl + alt + m; ctrl + alt + {p, s, a}
    {tglapp -- $TERMINAL -e spt, tglapp musescore, tglapp matlab}
ctrl + alt + o; ctrl + alt + v; ctrl + alt + p
    tglapp freetube
ctrl + super + {o, k, p}
    {obs --profile Default --collection default, obs --startrecording --profile Camera --collection camera, tglapp -a pavucontrol}
ctrl + alt + shift + b
    swp /home/xphyro/.local/share/applications/browser.desktop /home/xphyro/.local/share/applications/browser-other.desktop; statbarbrowser; statbarset
ctrl + alt + s; ctrl + alt + {c, e, a}
    {xdg-open ~/course-schedule.png, xdg-open "$( getloc exam )/main.pdf", openass}

#
##
### Programs ###

### System ###
##
#

{Caps_Lock, Num_Lock}
    xdotool key {Caps_Lock; statbarcaps, Num_Lock; statbarnum}; statbarset
# TODO:
# shift + super + alt + {comma, dot}
#     # PRESS shift + super + {comma, dot} AND super + {comma, dot}
super + {minus, equal, underscore, plus}
    {volch -5%, volch +5%, volmute, volset}; statbarvol; statbarset
super + ctrl + {minus, equal, underscore}
    {volappch -5%, volappch +5%, volmicmute; statbarvolmic; statbarset}
super + {F1, F2, F3}
    {todo, wowmacro toggle dmenu, volmicmute; statbarvolmic; statbarset}
super + alt + {minus, equal, underscore, plus}
    {brightch 5%-, brightch +5%, brightmute, brightset}
super + alt + ctrl + {underscore, plus, u, k, m}
    {xtrlock -b & sleep 1; xset dpms force suspend, xtrlock, yankunicode, kmcycle; statbarkm; statbarset, manpdf}
shift + super + alt + c; shift + super + alt + {h, l, j, k, o}
    mprisctl {prev, next, playpause, stop, open "$( etmprisuri --detect )"}
# shift + super + p
#     j4-dmenu-desktop
shift + super + alt + y; shift + super + alt + {y, u, s, p, a}
    {clplogynk, clpfilter uniqs, clpfilter sort, clpprint, yank}
shift + super + alt + {d, h, n, c}
    dunstctl {close, history-pop, set-paused toggle; statbarnotif; statbarset, context}
shift + super + alt + {k, p, F2, F3, F5, F7, F9, F10, F11, F12}
    {kasel, pssusp, syu, tpcycle; statbartp; statbarset, tglapp screenkey, restart-notifd, mountsel, umountsel, btccdev; statbarspeak; statbarvol; statbarset, btdcdev; statbarspeak; statbarvol; statbarset}
shift + super + alt + m
    view-music
ctrl + shift + super + alt + m
    view-music -c firefox
super + {KP_Divide, KP_Multiply}
    transset-df -a {--dec, --inc} 0.05
{XF86AudioLowerVolume, XF86AudioRaiseVolume}
    transset-df -a {--dec, --inc} 0.05
alt + F4
    xdotool key F12

#
##
### System ###
